using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
        print(transform.name + " : " + transform.position.ToString("F4"));
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
